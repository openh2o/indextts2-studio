# Development Tools.
.mypy_cache/
.ruff_cache/
__pycache__/
.idea/
.vscode/

# Environments.
.venv*/
venv*/
conda_env*/

# Python Bytecode.
*.py[cod]

# Distribution/Packaging.
/build/
/dist/
*.egg-info/
.pypirc

# Operating System Junk.
*.DS_Store
Thumbs.db
desktop.ini

# IndexTTS.
/cache/
/checkpoints/*
!/checkpoints/*.yaml
/outputs/
# 服务端运行时产物：参考音频缓存与日志，仅本机使用
/prompts/
/logs/
# BigVGAN CUDA 扩展的本地编译产物（torch.utils.cpp_extension 生成）
/indextts/s2mel/modules/bigvgan/alias_free_activation/cuda/build/

# Local tool scratch
.playwright-cli/
.workbuddy/
# GUI smoke-test evidence (kept locally, not part of the repo)
/gui-test-screenshots/

# Example audio files (downloaded on demand, not stored in git)
/examples/*.wav
/tests/sample_prompt.wav

.claude
.memsearch

# ---- 上传 GitHub 前追加 ----
# 本地 AI 工具目录
.zcode/

# 无关代码（服务运行不需要，本地保留）
/tests/
/cli_tests/
/tools/
/archive/
/gui-test-screenshots/
/docs/
/profile_vram.py

# 官方 README 宣传资源与 macOS 垃圾文件（服务不引用，server.py 的 /assets/ 路由服务的是 web/）
/assets/
# 仅 v1/CLI 批处理示例使用，Web 服务只用 examples/cases.jsonl
/examples/batch/
# 上游仓库的 CI / docker 发布流程，本仓库不适用
/.github/
# 上游 PyPI 打包清单，本仓库不发布
/MANIFEST.in

# IndexTTS v1 专属死代码（v2 推理链路 infer_v2.py 不引用，本地保留可随时恢复）
/indextts/infer.py
/indextts/cli.py
/indextts/gpt/model.py
/indextts/vqvae/
# 个人恢复笔记：含本机硬件/网络/路径等指纹信息，只保留在本地，绝不入库
/RECOVERY.md
